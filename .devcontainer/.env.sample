# Copy to .devcontainer/.env and customize. This file is safe to commit.
# Actual .env should be ignored (ensure .gitignore has .devcontainer/.env)
GIT_AUTHOR_NAME=Your Name
GIT_AUTHOR_EMAIL=you@example.com
